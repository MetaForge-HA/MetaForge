"""MetaForge CLI namespace.

Marker file so ``from cli.forge_cli ...`` resolves to the local
package even when site-packages contains an unrelated ``cli`` namespace
(e.g. installed transitively as part of ``stringzilla``).
"""
