# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
ENV/

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# IDEs
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Node (CLI)
node_modules/

# Environment
.env
.env.local
!.env.example

# Shell dotfiles
.bash_profile
.bashrc
.profile
.zprofile
.zshrc

# Git config (user-local)
.gitconfig
.gitmodules

# MCP config — `.mcp.json` is now version-controlled (MET-339): it's
# the launcher entry that ships the canonical Claude Code config so
# every contributor connects to the same MetaForge MCP server.
# Local overrides go in `.mcp.local.json` (gitignored below).
.mcp.local.json

# LightRAG local storage (working_dir for the L1 KnowledgeService)
.lightrag-storage/
.lightrag-spike-storage/

# Ripgrep config
.ripgreprc

# Bare git objects (WSL artifact — root and dashboard)
/HEAD
/config
/hooks
/objects
/refs
/dashboard/HEAD
/dashboard/config
/dashboard/hooks
/dashboard/objects
/dashboard/refs

# TypeScript build info
*.tsbuildinfo

# Vite build artifacts
vite.config.js
vite.config.d.ts

# Dashboard build output
dashboard/dist/

# Pytest temp dirs
pytest-of-*/

# Playwright
dashboard/playwright-report/
dashboard/test-results/

# Screenshots (test/debug artifacts)
*.png

# Playwright MCP logs
.playwright-mcp/
